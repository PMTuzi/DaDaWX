// WXS 辅助函数 - WXML 模板中可调用
function includes(arr, val) {
  if (!arr) return false
  for (var i = 0; i < arr.length; i++) {
    if (arr[i] === val) return true
  }
  return false
}

function toFixed1(num) {
  if (num === undefined || num === null || num === '' || num === '--') return '--'
  var n = parseFloat(num)
  if (isNaN(n)) return '--'
  return n.toFixed(1)
}

module.exports = {
  includes: includes,
  toFixed1: toFixed1
}
